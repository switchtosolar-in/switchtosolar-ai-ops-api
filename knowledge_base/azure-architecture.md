# Azure Architecture

SwitchToSolar is deployed on Microsoft Azure.

Components:

- Azure App Service for backend (Node.js)
- Azure App Service for frontend (Next.js)
- Azure SQL Database for structured data
- API routes handle communication between frontend and backend

The system uses environment-based configuration for dev, staging, and production.

Security and scaling are handled through Azure infrastructure.
